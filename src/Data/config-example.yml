user:
  name: John Doe
  nickname: Joe

storage:
  # Must be absolute path
  dir_data: /path/to/folder
  # dir_config: /path/to/folder
  dir_configs: /path/to/folder
  dir_resources: /path/to/folder

scheduler:
  placeholder: null

modules:
  blacklist:
    - null
    - null
  placeholder: null

server:
  ip: '192.168.0.31'
  port: 20
  enable: False

speechRecognition:
  apiKey: null
  placeholder: null